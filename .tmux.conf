# ----------------------------------------------------
# 1. 基本設定
# ----------------------------------------------------

# プレフィックスキーを C-a に変更
set -g prefix C-a
# デフォルトの C-b バインドを解除
unbind C-b
# C-a の重複押しで C-a を送信
bind C-a send-prefix
 
# マウス操作の有効化
set-option -g mouse on
 
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on

# ----------------------------------------------------
# 2. キーバインド
# ----------------------------------------------------

# ペインの縦分割 (prefix + |)
bind | split-window -h
# ペインの横分割 (prefix + -)
bind - split-window -v

# ペイン移動 (prefix + hjkl)
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# カスタムスクリプト
bind-key -n C-Space run-shell "$DOTFILES_DIR/bin/tmux-switcher.sh"

# ----------------------------------------------------
# 4. UI
# ----------------------------------------------------

# @see https://github.com/catppuccin/tmux/discussions/317#discussioncomment-11064512

# Configure Catppuccin
set -g @catppuccin_flavor "mocha"
set -g @catppuccin_status_background "none"
set -g @catppuccin_window_status_style "none"
set -g @catppuccin_pane_status_enabled "off"
set -g @catppuccin_pane_border_status "off"

# status left look and feel
set -g status-left-length 100
set -g status-left ""
set -ga status-left "#[bg=#{@thm_bg},fg=#{@thm_green}]  #S "
set -ga status-left "#[bg=default,fg=#{@thm_overlay_0},none] "
set -ga status-left "#[bg=#{@thm_yellow},fg=#{@thm_bg},bold]#{?window_zoomed_flag,  zoom ,}"

# status right look and feel
set -g status-right-length 100
set -g status-right ""
set -ga status-right "#[bg=default,fg=#{@thm_blue}] 󰭦 %Y-%m-%d 󰅐 %H:%M "
	
# Load catppuccin
run $DOTFILES_DIR/submodules/catppuccin/tmux/catppuccin.tmux

# Configure Tmux
set -g status-position top
set -g status-justify "absolute-centre"

# pane border look and feel
setw -g pane-border-status top
setw -g pane-border-format ""
setw -g pane-active-border-style "fg=#{@thm_overlay_0}"
setw -g pane-border-style "fg=#{@thm_surface_0}"
setw -g pane-border-lines single

# window look and feel
set -wg automatic-rename on
set -g automatic-rename-format "tmp"

set -g window-status-format ""
# set -g window-status-style "bg=#{@thm_bg},fg=#{@thm_rosewater}"
# set -g window-status-last-style "bg=#{@thm_bg},fg=#{@thm_peach}"
# set -g window-status-activity-style "bg=#{@thm_red},fg=#{@thm_bg}"
# set -g window-status-bell-style "bg=#{@thm_red},fg=#{@thm_bg},bold"
# set -gF window-status-separator "#[bg=#{@thm_bg},fg=#{@thm_overlay_0}]│"

# set -g window-status-current-format " #W "
set -g window-status-current-format "#{?client_prefix,#{#[bg=#{@thm_peach},fg=#{@thm_bg},bold] #W },#{#[bg=#{@thm_bg},fg=#{@thm_peach},bold] #W }}"

# popup
set -g popup-border-style "fg=#{@thm_overlay_0}"

